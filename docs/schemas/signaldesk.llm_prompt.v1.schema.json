{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:signaldesk:schema:llm_prompt:v1",
  "title": "SignalDesk LLM prompt payload v1",
  "description": "Guarded structured input contract for optional LLM explanations. The payload contains only a validated SignalDesk signal_card, fixed guardrails, untrusted-text labels, excluded-field labels, and the strict explanation output schema.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "task",
    "guardrails",
    "untrusted_provider_text_fields",
    "excluded_signal_card_fields",
    "signal_card",
    "output_schema"
  ],
  "properties": {
    "schema_version": {
      "const": "signaldesk.llm_prompt.v1"
    },
    "task": {
      "const": "explain_ta_signal_card"
    },
    "guardrails": {
      "type": "array",
      "minItems": 6,
      "items": {
        "type": "string",
        "minLength": 1,
        "pattern": "\\S"
      },
      "description": "Fixed instructions constraining the LLM to structured SignalDesk facts only."
    },
    "untrusted_provider_text_fields": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1,
        "pattern": "\\S"
      },
      "description": "Signal-card paths whose provider/news text must be treated as untrusted data."
    },
    "excluded_signal_card_fields": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1,
        "pattern": "\\S"
      },
      "description": "Signal-card fields intentionally removed or nulled before prompt construction."
    },
    "signal_card": {
      "type": "object",
      "description": "Canonical SignalDesk signal_card object validated by backend contracts before prompt construction. Narrative must be null."
    },
    "output_schema": {
      "type": "object",
      "description": "Strict signaldesk.llm_explanation.v1 JSON output schema."
    }
  }
}
